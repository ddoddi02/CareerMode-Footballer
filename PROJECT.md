# CareerMode-Footballer — 프로젝트 인수인계

> 다른 세션에서 이어서 작업하기 위한 문서. 진행 과정이 아니라 **현재 상태와 그렇게 결정한 이유**만 남긴다.

---

## 1. 무엇을 만들고 있는가

Football Manager와 FC 시리즈 커리어 모드를 섞은 게임. 경기 중에는 **선수 한 명만** 조작한다.

핵심 메커닉은 **시야 제한**이다. 화면은 선수가 바라보는 방향만 밝고 나머지는 어둡다 (Escape from Duckov 방식). 뒤를 보려면 고개를 돌려야 하고, 고개를 돌리는 동안은 공을 못 본다.

FM 능력치는 이 시야와 직접 연결될 예정이다 — Vision은 시야각, Anticipation은 잔상 유지 시간, Composure는 압박 시 시야 축소 저항. 커리어 모드에서 능력치가 오르면 **숫자가 아니라 화면이 실제로 넓어진다.**

### 프로토타입이 검증하려는 것

HUD 좌측 상단에 항상 떠 있는 두 숫자가 이 프로젝트의 존재 이유다.

```
확인 후    12/18  (67%)
블라인드    4/15  (27%)
```

터치 직전에 수비수를 확인했을 때와 확인하지 못했을 때의 성공률. **이 격차가 벌어지면 시야 메커닉이 작동하는 것이고, 붙어 있으면 장식일 뿐이므로 설계를 다시 해야 한다.** 20~30 라운드 돌려서 이 숫자부터 볼 것.

---

## 2. 환경

| | |
|---|---|
| Unity | 6000.4.1f1 |
| 렌더 파이프라인 | URP 17.4.0 |
| 입력 | Input System 1.19 (레거시 폴백 코드 있음) |
| 편집 | Unity MCP (`com.coplaydev.unity-mcp`) 로 원격 작업 |

Unity 6에서 이름이 바뀐 API를 쓴다: `linearVelocity`, `linearDamping`, `angularDamping`, `PhysicsMaterial`. 구 이름은 전부 Obsolete.

---

## 3. 핵심 설계 결정 — 되돌리지 말 것

각 항목은 대안을 검토하고 내린 결론이다. 이유를 모르면 되돌리기 쉬우므로 이유를 함께 적는다.

### 3.1 2D가 아니라 3D 프로젝트

축구의 시야 제한은 좀보이드와 달리 **지형 차폐가 아니라 목이 180도밖에 안 돌아가는 문제**다. URP 2D Renderer의 Shadow Caster 2D가 줄 이점이 없다. 반대로 3D는 방향별 스프라이트 제작 비용(8방향 × 애니메이션 10종)을 통째로 없애고, 공의 높이(크로스·헤딩)를 공짜로 준다. 픽셀 룩은 저해상도 렌더 타겟 + 팔레트 셰이더로 확보한다.

### 3.2 시야 오버레이는 Renderer Feature 없이 쿼드 하나

카메라 자식으로 붙인 쿼드에 셰이더를 물린다. **직교 카메라라 모든 시선 광선이 카메라 forward와 평행**이므로, 광선-평면 교차 한 번으로 각 픽셀의 지면 좌표가 나온다. 뎁스 버퍼도 Renderer Feature 설정도 필요 없다.

주의: Linear 컬러스페이스라 sRGB 변환 때문에 알파를 0.97 근처까지 올려야 실제로 검게 보인다.

### 3.3 인지(게임 로직)와 암전(렌더링)은 분리

`PerceptionSystem`이 `Clear / Peripheral / Unknown`을 CPU에서 판정하고, 셰이더는 화면을 어둡게 할 뿐이다. AI·리플레이·세이브가 전부 같은 데이터를 쓴다. 안 보이는 선수는 렌더러가 꺼지고 마지막 목격 위치에 **4.5초간 페이드되는 잔상**이 남는다.

말초시에서는 **움직이는 대상만** 보인다. 등 뒤에 가만히 붙은 수비수는 안 잡히고 움직이는 순간 걸린다.

### 3.4 리시브 구속 — FC의 실제 규칙 2모드

공이 날아오는 동안 이동을 제한한다. 매 프레임 **공의 남은 경로에 대한 수직 거리**로 모드를 판정한다 (히스테리시스 진입 2.4m / 이탈 3.2m).

| 모드 | 조건 | 허용되는 움직임 |
|---|---|---|
| **OnLine** | 공이 몸쪽으로 옴 | 공의 진행 축 성분만. 마중 or 흘리며 후퇴 |
| **Chase** | 공이 다른 데로 감 | 입력 방향 무시, 무조건 공 쪽으로 |

검증됨: OnLine에서 측면 입력은 이동 0.00m, Chase에서 정반대 키를 눌러도 이동 벡터가 공 방향과 일치.

**이 구조 때문에 패스 이후 선택지가 사라진다.** 그래서 판단이 두 지점으로 몰린다 — 패스 전(어느 방향으로 달릴 것인가)과 터치 순간(무엇을 누를 것인가). 그 사이 1초가 통째로 **뒤를 확인하는 시간**이 된다. FC에서는 비어 있는 그 1초가 여기서는 유일한 행동이 되는 것이 설계 의도다.

### 3.5 리드 패스 = 주자 속도 × 공 비행 시간

> 고정 패서는 없어졌다(§3.17). 이 계산은 그대로 살아서 `PassPlanner.LeadPoint()`로
> 옮겼고, 이제 **모든 패스**가 이 식을 쓴다.

**받는 사람의 진행 방향과 속도를 읽어 그 앞 공간으로 배급**한다.

페이스를 유지하면 공과 정확히 같은 지점에서 만나도록 계산된다. 걷기·조깅·측면·대각 전부 도착 시각 오차 0.03초 이내로 검증됨.

**스루패스는 더 세게 찬다** (`4 + 속도 × 2.2`). 고정 속도로는 스프린터(7.9 m/s)를 공(11.5 m/s)이 따라잡는 데 28m 리드가 필요해서 필드에 안 들어간다. 실제 축구에서 스루패스를 강하게 차는 이유와 같다.

### 3.6 공 물리 — 등속+하드스톱이 아니라 초기속도+구름마찰

이전 방식은 도착 시각을 먼저 정하고 등속 벡터를 역산한 뒤 타이머가 끝나면 속도를 0에 대입했다. 감속 구간이 없어서 공이 뚝 멈췄다.

지금은 순수하게 마찰이다. 스크립트에 "멈출 지점"이라는 데이터가 없다.

```
v(t) = v0 - a·t                    a = rollDecel = 5 m/s²
D(t) = v0·t - a·t²/2

T  = (v0 - √(v0² - 2aD)) / a       Ball.TravelTime
v0 = √(v_도착² + 2aD)               Ball.SpeedToReach
```

해석해와 `Update()` 수치적분을 대조해 오차 0.5% 이내 확인됨. 리드 패스 계산이 이 닫힌 해 위에 서 있으므로 **여기를 Rigidbody로 바꾸면 예측이 깨진다.**

**포물선은 다시 붙였다 — 단, 굴림과 섞지 않고 앞뒤로 이어 붙인다.** 롱패스(§3.17)와
크로스는 사람 머리를 넘겨야 하므로 띄우는 것 자체가 목적이다.

체공 중에는 구름 마찰이 안 걸리는 것이 문제가 아니라 **정답**이다. 뜬 공에는 잔디가
안 닿으므로 마찰항이 없는 게 물리적으로 맞고, 그래서 비행이 여전히 닫힌 형태로 풀린다.

```
입력은 발사각이 아니라 정점 높이 h        ← 패스의 목적이 "머리 위로 넘기기"이므로
T    = 2√(2h/g)                          체공 시간
v_수평 = D / T
착지 후: 수평 속도 × bounceKeep(0.62) 로 §3.6의 구름 모델에 그대로 인계
```

각도 대신 정점을 입력으로 둔 이유가 이것이다. 각도는 "얼마나 세게"와 "얼마나 높이"가
섞이지만, 정점은 **넘어야 할 대상의 키**라는 하나의 물리량이다. 수비수가 0.8초 뒤 공
위치를 물어보면(§3.18) 체공 구간도 착지 후 구름 구간도 전부 답이 나온다.

### 3.7 공 물리를 정교하게 만들지 않는다

Unity 내장 물리에는 마그누스 효과도, v² 항력도, 구름 저항 파라미터도 없다 (Rigidbody에 rolling friction 없음을 리플렉션으로 확인함). 붙이려면 `FixedUpdate`에서 직접 계산해야 한다.

**하지만 붙이지 않기로 했다.** 물리가 정교할수록 스탯이 시뮬레이션 노이즈에 묻히고, 패싱 12와 16의 차이를 플레이어가 못 느낀다. 커리어 모드에서 그 차이가 곧 게임이다.

회전도 각속도를 시뮬레이션하지 말고 **"비행 중 옆으로 몇 미터 휘는가"**로 직접 정할 것. 등가속 횡력 하나로 환산되어 궤적이 여전히 닫힌 형태로 풀린다.

### 3.8 스탯은 파워보다 오차를 지배한다

누구나 20 m/s로 찬다. 차이는 그게 발밑에 꽂히느냐 2m 옆으로 가느냐다.

| | 스탯 영향 폭 |
|---|---|
| 속도 | ±15% (좁게) — 넘어가면 파워가 스탯 체크가 됨 |
| 방향 오차 | 넓게 |
| 강약 오차 | 넓게 |

**두 오차의 원인이 다르다:**

- 방향이 어긋나는 원인은 **압박** (거리 아님)
- 강약이 어긋나는 원인은 **거리**

### 3.9 방향 오차는 각도가 아니라 미터로 정의

각도로 정의하면 횡편차가 `거리 × tan(각도)`로 이미 거리에 비례하는데 난이도에도 거리항을 넣으면 **거리가 두 번 곱해진다.** 실제로 그렇게 짰다가 거리 5배에 편차 9배가 나왔다.

```
aimError_m = 5.0 × (1-stat) × difficulty × (0.4 + 0.6·d/30) × Bell()
θ = atan(aimError_m / dist)
```

`Bell()`은 균등분포가 아니라 정규분포 근사(uniform 3개 합)다. 대부분 정확하고 가끔 크게 빗나가야 축구답다.

실측 (자유 상황, 평균):

| 거리 | 스탯18 | 스탯12 | 스탯6 | 스탯6 거리대비 |
|---|---|---|---|---|
| 10m | 0.08m | 0.33m | 0.57m | 5.7% |
| 30m | 0.14m | 0.54m | 0.96m | 3.2% |
| 50m | 0.19m | 0.74m | 1.35m | 2.6% |

거리가 늘어도 상대 정확도는 좋아진다. 압박이 붙으면 30m에서 스탯6이 0.98m → 2.87m로 벌어진다.

**강약 오차 계수는 작게 (0.18).** 구름 모델에서 도달 거리가 `v²/2a`이므로 속도 오차 10%가 거리 오차 21%로 제곱 증폭된다.

### 3.10 드리블 소유 — 터치 거리가 속도에 따라 변한다

공을 받으면 몸에 붙고, 진행 방향 앞에 놓인다. **얼마나 앞인지가 드리블의 위험/보상 전부다.**

선형 보간으로 하면 일반 주행에서 이미 1.23m가 나와 상시 노출된다. 지수 곡선(`touchCurve = 2.5`)으로 전력질주할 때만 밀려나게 했다.

| 속도 | 터치 거리 | 수비 발(1.0m) |
|---|---|---|
| 정지 | 0.40m | 안전 |
| 조깅 3.2 | 0.51m | 안전 |
| 주행 5.6 | 0.84m | 안전 |
| **전력 7.9** | **1.45m** | **닿음** |

드리블 중에는 `TrackBall`을 꺼서 시선이 진행 방향을 따라간다. 자기 발밑을 쳐다보고 있으면 안 된다.

### 3.11 태클 — 두 조건 AND

```
① 수비수 → 공 거리 ≤ 1.0m
② 공과 수비수 사이에 공격수 몸이 없음   (선분 투영, 반경 0.45m)
둘 다 충족 → 성공
```

**드리블러와 공의 거리는 성공 조건이 아니다.** 그 값(`Ball.Exposure`)은 수비수가 언제 덤빌지 고르는 AI 트리거일 뿐이다. 검증: 터치 2.0m라도 수비가 공에 못 닿으면 실패, 터치 0.5m라도 골사이드 잡히면 뺏김.

원안의 `else {fail}`은 실드하는 공격수를 무적으로 만들어 "영원히 실드"가 최적해가 된다. 그래서 경합은 주사위로 바꿨다.

- 몸이 사이에 있고 저항 중(이동 또는 E) → 태클/힘 스탯으로 굴림
- **실패한 태클은 파울이 될 수 있다** — 대가가 없으면 태클 난사가 최적해. 뒤에서 들어가면 24.9%, 앞에서 7.0%
- 태클 쿨다운 1.1초, 실패 시 1.0초 넘어짐. 공에 못 닿은 헛발질도 동일하게 처벌

### 3.12 전술 존은 콜라이더가 아니라 데이터 모델 (미구현, 결정만)

봇 AI를 존 기반으로 만들 계획이므로 콜라이더가 유리해 보이지만 **반대다.**

AI가 존에 대해 하는 일은 충돌이 아니라 **질의**다. "어느 존으로 가야 하나", "오른쪽 하프스페이스가 비었나" 같은 질문은 전부 **자기가 없는 존**에 대한 조회다. 트리거는 답할 수 없다.

게다가 현대 축구의 존은 볼 위치에 따라 슬라이드한다. 콜라이더면 매 프레임 20~30개를 움직여야 하고, 물리 트리거 순서는 결정적이지 않아 FM식 리플레이 재현이 깨진다. `Teleport()`가 CharacterController를 껐다 켜는 것도 트리거 상태를 어긋나게 한다.

**대신 5레인 × 6밴드 전술 그리드**를 만들 것. 존 판정은 산술 두 줄(O(1)), 행동 정의는 ScriptableObject, 시각화는 `OnDrawGizmos`. 사이드라인 판정도 여기로 옮기면 두 줄로 줄고 AI가 같은 API를 쓴다.

**밴드는 들어왔다** (`TacticalPitch.BandOf` / `CellOf`). 롱패스가 "수비 숫자가 가장 적은
지역"을 골라야 하는데(§3.17), 그 질문은 **몸이 아니라 칸에 대한 질문**이라 그리드가
있어야 답할 수 있었다. 비어 있음은 셀의 성질이지 선수의 성질이 아니다.
행동 정의(ScriptableObject)와 기즈모는 아직 없다.

### 3.13 포메이션은 피벗 기준 오프셋 (AI 미구현, 배치만)

`Formation.cs`에 4-1-2-3 슬롯을 데이터로만 정의한다. 좌표 기준점은 하프라인이 아니라
**원 볼란치(DM)**다. 사람이 조작하는 슬롯이 원 볼란치이고 드릴이 그를 원점에서
시작시키므로, 피벗을 기준으로 삼으면 드릴 기하가 그대로 살아 있는 채로 나머지 열 명이
주위에 배치된다.

두 가지 예외:

- **골키퍼**는 블록이 아니라 **자기 골라인 기준 5m**. 블록 상대 오프셋으로 두면 팀이
  밀려 있을 때 골대 밖으로 나간다.
- **필드 플레이어는 자기 페널티박스 안에 서지 않는다** (`|z| ≤ 36`으로 클램프).
  없으면 밀려 있는 원정팀 센터백이 자기 골키퍼 위에 겹친다. 클램프가 걸리면 백4가
  박스 라인에 일자로 서는데, 깊이 내려앉은 팀의 실제 모습이라 그대로 둔다.

기존 드릴 오브젝트 3개는 복제하지 않고 각자 슬롯을 차지한다 —
`Player` → 홈 DM, `Passer` → 홈 LCB, `Defender` → 원정 ST.
나중에 §3.12 전술 그리드가 생기면 미터가 아니라 레인/밴드로 표현할 것.

### 3.14 사이드라인 판정은 수식으로 (콜라이더 X)

거리가 **연속값**으로 필요하다. 이진 판정만으로는 "라인에 가까울수록 점진적으로" 같은 걸 못 만든다.

넓은 쪽 판정은 신발끈 공식을 쓰고 있지만 **해석적으로 정리된다.** 삼각형 넓이로 환원:

```
A_right = 2·hz·(hx - gx) - hz·(px - gx)
A_left  = 2·hz·(hx + gx) + hz·(px - gx)
→ 넓은 쪽 = sign(gx - px)
```

지금 코드는 신발끈 20줄이라 이 2줄로 줄일 여지가 있다 (미적용).

### 3.15 수비 AI — 공통 원칙 + 포지션별 임무

**분리가 핵심이다.** 모든 수비수가 같은 간격으로 서고 같은 라인까지 내려가지만,
**아무도 같은 사람을 보고 있지 않다.** 그래서 공유되는 것(`TeamDefence`)과 포지션마다
다른 것(`DefenceDuties`)을 파일로 나눴다.

#### 공통 원칙 — 적용 순서가 곧 우선순위 (뒤가 앞을 덮어씀)

```
1 슬롯 배정 (공 쪽으로 블록 전체 슬라이드)
2 이웃과의 간격 유지 — 벌어진 선수를 당겨 공간을 좁힌다
3 최종 라인은 센터백. 센터백이 내려갈 수 있는 한계에 닿으면 블록 전체가 멈춘다
4 자기 역할이 책임지는 상대를 잡고, 그가 내 영역에 들어오면 붙는다
5 역습이면 박스에 닿기 전까지 가지 않는다 — 지연한다
6 4번 때문에 생긴 구멍을 메운다
7 그리고 포지션을 벗어나지 않는다 — 4·5·6이 낸 답을 자기 슬롯 반경 안으로 클램프
```

**7번이 마지막이라서 최우선이다.** 위의 전부가 "포지션을 벗어날 이유"이고 전부 허용되지만,
**거기까지만** 허용된다. 상대를 어디까지든 따라가는 수비는 모양이 없는 게 아니라
**10개의 개별 추격**이고, 그때 뒤에 열리는 공간이 이긴 경합들보다 비싸다.

목줄은 역할별이다 (`Formation.PositionLeash`): 센터백 9m · 사이드백 12m · 미드필더 13m ·
윙어 15m · 스트라이커 16m. 뒤가 짧고 앞이 긴 이유는 **이탈의 대가를 치르는 곳이 뒤**이기
때문이다. 스트라이커가 12m 벗어나면 압박 하나를 망친 것이고, 센터백이 12m 벗어나면
골문을 연 것이다. `leashScale` 로 전체를 조인다.

거부가 아니라 **클램프**다 — 그는 여전히 마크하러 가고, 커버하러 나가고, 역습을 지연한다.
줄이 짧아서 자기 포지션 끝에서 멈출 뿐이다. 상대에게 못 닿는 수비수가 실제로 하는 행동이
그거고, 그래서 모양이 살아남으면서도 임무가 죽지 않는다.

**측정 (같은 상황, 토글만 바꿈):**

| | 평균 이탈 | 최대 이탈 | 담당 배정 |
|---|---|---|---|
| 7번 ON | **4.44m** | **9.14m** | 8명 |
| 7번 OFF | 7.05m | 14.30m (윙어) | 5명 |

*이탈 = 스테이션에서 블록 공통 shift를 뺀 잔차. 즉 순수하게 "모양에서 벗어난 정도".*

담당 배정이 **줄지 않고 늘었다**(5→8). 목줄이 마킹을 막을 거라 예상했는데 반대였다 —
7번은 커버 단계 앞에도 걸리므로, 클램프된 선수는 `pulled` 가 해제되어 **"비운 적 없는 슬롯"을
메우러 두 번째 선수가 나가는 일이 사라진다.** 그 두 번째 선수가 자기 담당을 잡을 여유가 생긴다.

**주의 — 7번은 커버(6)보다 먼저도 한 번 돌아야 한다.** 커버는 `pulled`를 보고 "누가 슬롯을
비웠나"를 판단하는데, 목줄에 걸려 되돌아온 선수는 **비운 적이 없다.** 없는 구멍을 메우러
두 번째 선수를 내보내면 이미 사람이 서 있는 자리에 하나가 더 간다. 그래서 `HoldPosition()`을
4번 뒤와 5번 뒤 **두 번** 호출한다 (멱등이라 안전).

**7번이 건드리지 않는 것: 이미 날아가는 공의 차단**(§3.18). 그건 서 있는 위치가 아니라
움직이는 물체에 대한 실시간 반응이고, 한 명에게만 주어지며, "물리적으로 도달 가능한가"로
이미 제한되어 있다.

**3번이 왜 별도 원칙인가.** 각자를 개별로 클램프하면 **블록이 찌그러진다** — 백4가 멈춘
뒤에도 앞 선수들은 계속 내려오기 때문이다. 라인은 *전체*를 멈춰야 하므로, 클램프가
개별 스테이션이 아니라 **공유 shift에서 빠져나가야** 한다. `ShapeShift()`가 센터백이
어디에 서게 될지를 먼저 보고, 바닥을 넘으면 shift 자체를 깎는다. 검증: `LineOnFloor=true`
일 때 센터백 z=44.4 (골라인 52.5 − `backLineFloor` 8 = 44.5), 블록 폭 31.9m 유지.

**4번 — 담당을 받는 것과 쫓아가는 것은 다르다.** 내 영역 안이면 마킹(골사이드로 붙음),
밖이면 **주시만 한다**(`watchBlend` 0.35로 그쪽으로 기울 뿐). 이 구분이 블록과 10개의
1대1을 가르는 전부이고, 그래서 전원에게 담당을 배정해도 안전하다. `maxMarkTravel` 26m가
목줄이다 — 없으면 사이드백이 윙어를 따라 반대편까지 가면서 모양을 통째로 끌고 간다.

**5번 — 역습에 달려드는 것은 수비가 아니다.** 백4가 아직 복귀 중인데 달려나가면
원터치에 뚫린다. 그가 원하는 게 그거다. 그래서 박스 도달 전까지는:
서서 지연 · 안쪽 차단 · 뒤 선수로 가는 패스 길목 · 슛 각도 축소 — **전부 한 점**이다.
`ContainStation()` 하나가 네 가지를 동시에 만족시킨다. 검증:

```
break=True  carrier=(-9, 29)  박스 밖
presser mode=Contain  station=(-3.1, 32.1)
  골사이드 ✓   간격 7.0m (denyGap 1.05가 아님) ✓
  안쪽으로 shade |-3.1| < |-9| ✓   태클 안 함 ✓
```

`WantsTackle()`이 Contain 중에는 무조건 false를 반환한다. **지연과 태클은 반대말이다** —
서라고 지시받은 사람이 달려들면 지연이 벌어준 시간이 통째로 사라진다.

#### 포지션별 임무 (`DefenceDuties.cs`)

| | 담당 |
|---|---|
| 스트라이커 | 강한 압박: 빌드업 하는 자(GK·CB·FB), **공 가진 사람 우선**. 약한 압박: 내려와서 미드필더 |
| 윙어 | 자기 쪽 사이드백. 단 약한 압박이면 스트라이커와 함께 내려와 미드필더 |
| 미드필더 | 자기 영역 안의 상대 미드필더 |
| 센터백 | 하나가 스트라이커를 잡으면 **나머지는 다른 공격수를 잡지 않는다** — 박스로 들어오는 위험한 선수만 본다 |
| 사이드백 | 자기 쪽 윙어 |

**아무도 두 번 배정되지 않는다.** 한 번 잡힌 상대는 판에서 빠진다. 없으면 "각자 가장
가까운 사람"이 항상 그렇듯 **백4 전원이 같은 스트라이커를 마크**하는 것으로 수렴한다.

**주의 — 남는 센터백은 "가장 앞선 선수"가 아니라 "가장 위험한 선수"를 본다.**
처음엔 골에 가장 가까운 미배정자를 골랐는데, 그게 보통 **터치라인의 윙어**였다.
센터백이 중앙을 비우고 이미 사이드백이 커버 중인 채널로 나가버리고, 침투하는 선수는
바로 그 구멍으로 들어온다. `Danger()` 0.30 미만이면 **아무도 안 잡는다** — 남는 센터백은
남아 있으라고 있는 것이다. 수정 후 사이드백이 윙어를 2.3m에서 잡았다(이전엔 센터백이
가져가 사이드백이 놀았다).

**주의 — 마킹 대상만 바꾸면 압박이 안 내려온다.** 약한 압박에서 스트라이커에게
미드필더를 담당시켰는데 위치는 하프라인 그대로였다. 멀리서 쳐다볼 뿐이었다.
`forwardDrop` 9m로 전방 3명의 스테이션 자체를 내려야 한다. 검증: Mid에서 전방 평균
z=0.3 → Low에서 z=18.4.

**정보는 여전히 지연된다** (§3.15 이전 판과 동일). `PitchIntel`이 1초에 한 번만 그림을
갱신하고 그 사이에는 낡은 그림으로 움직인다. 압박 강도(`PressIntensity`)도 스냅샷 공
위치로 판정하므로 최대 1.8초 늦다 — 의도된 것이다. 실시간인 것은 §3.18 차단뿐이다.

### 3.16 공격 AI — 규칙이 아니라 점수

`TeamAttack`이 오프더볼 선수마다 **자기 존 안의 후보 지점들을 점수화**해서 최선을 고른다.
if/else 규칙으로 짜지 않은 이유: 5원칙이 서로 다른 방향으로 당기기 때문이다.
침투(4)와 삼각형 유지(1)와 포지션 지키기(2)는 동시에 만족될 수 없을 때가 많고,
규칙으로 짜면 우선순위 사다리가 되어 하위 원칙이 영원히 죽는다. 전부 항으로 두면
가장 많이 만족시키는 지점이 이긴다.

```
lane      패스 길이 열려 있는가        (1)
range     패스 가능 거리인가 7~24m      (1)
triangle  캐리어에서 본 각도가 60°인가  (1)
space     가장 가까운 수비수와의 거리   (2)
zone      자기 슬롯에서 얼마나 벗어났나 (2, 음수 가중)
unstick   지금 뚫으려는 방향과 일치하나 (3)
run       오프사이드 라인에 얼마나 근접 (4)
show      곤경에 빠진 동료와의 거리     (5)
```

후보 배치는 `Random`이 아니라 **황금각 나선**이다. §3.6의 결정성·리플레이 재현을
깨지 않기 위해서다.

**오프사이드는 플래그가 아니라 좌표 클램프다.** `Offside.KeepOnside()`가 후보 지점을
라인 안쪽으로 당긴다. 침투(4)는 "라인을 넘어가라"가 아니라 "라인에 최대한 붙어라"로
구현된다 — 실제로 스트라이커가 하는 것도 그거다. 심판이 생기면 그때 플래그를 만든다.

**주의 — 캐리어에게 붙은 압박수는 패스 길을 막는 게 아니다.** 처음엔 캐리어 발밑부터
선분 거리를 쟀는데, 압박수 한 명이 모든 패스 선분의 시작점 근처에 있으니
**8명 전원의 lane이 0.8m로 나와 팀 전체가 영구 '막힘' 상태**가 됐다.
`carrierShadow`(2.5m)만큼 선분 앞부분을 잘라내야 한다. 그는 사람에 대한 압박이지
특정 패스 각도를 덮고 있는 게 아니다.

### 3.17 패스 선택 — 게이트는 사다리, 그 다음은 점수

> **이전 버전은 "가장 가까운 사람부터" 사다리였다. 그게 문제였다.**
> 사다리는 첫 기준에게 나머지 전부에 대한 거부권을 준다. 더 좋은 공이 있는데도 옆 사람에게
> 주고, 코드 어디에도 "그건 아닌데"라고 말할 자리가 없었다. §3.16이 오프더볼에 대해 이미
> 내린 결론과 같은 실수였다.

`PassPlanner.Choose()`는 **게이트 → 점수** 2단이다.

**게이트 (통과 못 하면 후보가 아니다)**

```
레인      좌우 2m 안에 수비수가 있으면 탈락
사거리    5m ~ 30m
밀집도    받는 사람 블록의 수비수 ≤ maxCellDefenders  (기본 3 = 아주 느슨. 캘리브레이션용)
```

**레인은 항이 아니라 게이트로 남는다.** 몸에 박히는 패스는 "조금 나쁜 패스"가 아니라
턴오버다. 위협 점수가 높다고 사람을 뚫고 갈 수 있게 하면 레인 판정 자체가 무의미해진다.

**점수 (통과한 것들 중에서)**

```
threat    위협적인 위치인가            + 1.00
quality   나보다 잘 차는 사람인가       + 0.55
freedom   여유가 있는가 (본인 + 블록)   + 0.80
risk      비행 시간                    − 0.70
```

**거리는 항이 아니다.** `risk`(비행 시간)를 통해서만 들어온다. 그게 수정의 핵심이다 —
가까움은 이제 **순서가 아니라 비용**이다.

**주의 — `quality`는 "나보다 얼마나 나은가"다.** `max(0, 상대−나)/qualitySpan`이므로 내가
팀 최고면 자동으로 0이 된다. "본인이 가장 높다면 다음으로 넘어간다"가 특수 케이스 없이
그냥 떨어진다. 대신 **선수별 패스 능력치가 실제로 달라야** 살아난다 —
`Formation.PassingFor(Role)`. 팀 전체가 한 값이면 이 항은 항상 0이고 죽은 무게다.
(실제로 씬에 기본값 0.65가 그대로 들어가 있어서 항이 통째로 죽어 있었다.)

**주의 — `Danger()`만으로는 threat이 피치 대부분에서 0이다.** `Danger()`는 "여기서 실점하면
얼마나 아픈가"라서 자기 진영에서는 옆으로 빼는 공이나 15m 전진 패스나 똑같이 0.00이 나온다.
그러면 점수가 `freedom − risk`로 붕괴하고 **결국 가장 짧은 공이 이긴다 — 없앤 사다리가
그대로 부활한다.** `progressionShare`(골대까지 거리가 얼마나 줄어드는가)가 최종 3분의 1
바깥에서 이 항을 지탱한다.

**주의 — `riskTime`은 실제 최장 패스보다 커야 한다.** 2.0초로 뒀더니 16m 넘는 공이 전부
1.00으로 포화돼서 항이 후보를 구분하지 못했고, 다시 최단 거리가 이겼다. 3.2초로 올려야
0.30~1.00 범위로 벌어진다. **포화된 항은 없는 항과 같다.**

**나머지 (롱패스)**: 게이트를 통과한 후보가 하나도 없으면 그리드 셀 기준 수비가 가장 적은
곳의 사람에게 띄워 보낸다 (동률이면 더 앞). 여기만 순서 판정으로 남는데, 묻는 게 하나뿐이기
때문이다 — 어디가 비었나. 높이가 나머지를 전부 답해준다(§3.18).

**2번이 "패스가 수비수를 통과해서 도착하던" 문제의 전부다.** 공을 물리적으로 막는 코드는
지금도 없다. 대신 **사람이 서 있는 길로는 애초에 보내지 않고**, 그래도 근처로 가는 공은
수비가 가져간다(§3.18). 막는 게 아니라 안 보내는 것이 맞는 모델인 이유는, 축구에서 패스가
차단되는 건 공이 몸에 맞아서가 아니라 **그 각도를 이미 포기했어야 했기 때문**이다.

`carrierShadow`(2.5m)는 §3.16에서 배운 것을 그대로 가져왔다. 패서에게 붙은 압박수는 모든
패스 선분의 시작점 근처에 있으므로, 선분 앞부분을 잘라내지 않으면 **모든 패스가 막힘으로
판정된다.**

**3번은 왜 띄우는가.** 30m 안이 전부 막혔다는 것은 그 사이에 사람이 가득하다는 뜻이므로,
땅볼로 더 길게 차면 더 많은 사람을 통과해야 한다. 높이는 그 사람들을 한 번에 무효화하는
유일한 축이고, §3.18의 차단 판정이 머리 높이(1.1m)로 잘려 있어서 **"뜬 공은 못 뺏긴다"는
규칙을 따로 쓰지 않아도 그렇게 동작한다.**

**주의 — 패스 대상은 캐시한다.** 매 프레임 다시 고르게 했더니 최선 후보가 두 명 사이에서
깜빡이면서 패서가 제자리에서 팽이처럼 돌았다. 몸을 돌리기 시작하면 그 패스에 커밋한다
(`alignAngle` 40°, 못 돌리면 `maxTurnWait` 0.9초 뒤 그냥 참 — 대신 난이도 페널티).

**골키퍼는 패스 후보에서 뺐다.** 받을 두뇌가 없어서 공이 그냥 지나가 죽는다.

**주의 — 패스가 도착하는 것과 받는 것은 다르다.** 공은 미터 단위 오차를 안고 날아가므로
(§3.8) 받는 사람 **옆에** 떨어지지 실제로 그의 발에 붙지 않는다. 루즈볼을 주우러 가는
로직이 없으면 공이 2m 옆에 멈춘 채 소유가 끊기고 라운드가 DEAD BALL로 죽는다.
그래서 `TeamAttack.TickChase()`가 **한 명만** 보낸다 — 패스를 받기로 한 사람이 우선,
안 되면 가장 가까운 사람. 목적지는 공의 현재 위치가 아니라 §3.18과 같은 방식으로 구한
**도달 가능한 첫 지점 또는 정지 지점**이다.

**주의 — "공에 가장 가까운 팀원"을 캐리어로 취급하면 안 된다.** `Recompute`가 캐리어에게
"제자리 유지"를 주는데, 루즈볼일 때 최근접 팀원을 캐리어로 읽으면 **그 사람이 공 옆에
얼어붙는다.** 공을 주우러 가야 할 바로 그 사람이 말이다. 실제로 센터백 한 명이 완전히
정지한 채 공이 2m 옆에 굴러다니는 증상으로 나타났다. 캐리어는 **실제로 공을 가진 사람**
뿐이다 (`BallCarrier`).

### 3.18 차단 — 공을 쫓는 게 아니라 공이 갈 곳에 서 있는 것

`TeamDefence.TickIntercept()`가 공의 미래 경로(§3.6의 닫힌 해)를 0.08초 간격으로 걸어가며
"이 시각 이 지점에 도달 가능한 사람이 있는가"를 묻는다. 처음 참이 되는 지점이 차단점이다.

```
need = 거리/interceptSpeed + reaction(0.28s)   ≤   t 이면 도달 가능
```

**공의 현재 위치를 쫓게 하면 절대 못 잡는다.** 항상 한 발 늦기 때문이다. 이건 예측이
아니라 축구에서 말하는 "읽기"이고, `reaction`이 곧 읽는 데 걸리는 시간이다.

**여기만 실시간이다.** 나머지는 전부 1초짜리 스냅샷(§3.15)으로 움직이지만, 이미 날아가고
있는 공은 그 수비수가 **직접 관여 중인 대상**이다. 1초 낡은 그림으로 패스를 끊을 수는 없다.

**한 명만 보낸다.** §3.15의 원칙 5와 같은 이유 — 느슨한 공에 블록 전체가 끌려가면 그 뒤가
통째로 열린다. 그리고 한 명이 도착하면 그걸로 끝난다.

**뜬 공은 자동으로 제외된다.** 샘플 높이가 `interceptHeight`(1.1m)를 넘으면 건너뛴다.
규칙을 쓴 게 아니라 기하가 그렇게 되어 있다.

**주의 — 패스 직후 1.4m는 아무도 못 가져간다** (`passEscape`). 없으면 패서를 마크하던
수비수가 발에서 공이 떠나기도 전에 모든 패스를 먹는다. 그건 차단이 아니라 그냥 옆에 서
있던 것이다.

### 3.19 플레이어의 패스는 방향이 아니라 사람을 고른다

방향키 입력대로 공을 차면 축구가 아니라 볼링이다. `PassPlanner.PickInCone()`은 입력 방향을
중심으로 **±15°(`coneHalfAngle`, 인스펙터 조정) 웨지 안에서 가장 가까운 팀원**을 찾아 그
사람에게 보낸다.

**웨지가 비어 있으면 입력 방향 그대로 찬다. 이건 폴백이 아니라 실수 판정이다.** 아무도
뛰어들지 않는 공간으로 보낸 공이고, 그 공은 §3.18에 그대로 먹힌다. 화면에도
"그 방향에 사람이 없습니다"로 뜬다.

W(스루)는 같은 사람 선택 위에 §3.5의 리드를 얹고 `Offside.KeepOnside()`로 당긴다.

### 3.20 디버그 뷰 — 거리는 화면에서 읽을 수 없다

직교 카메라가 90m 위에 있어서 **깊이 단서가 없다.** 1m 옆의 수비수와 5m 옆의 수비수가
화면에서 거의 같아 보이므로 "저 수비수 바로 옆인데 왜 패스가 나가지"라는 판단 자체가
화면으로는 불가능하다. 그래서 판정 거리를 **잔디에 원과 회랑으로** 그린다 (`G`).

| 표시 | 값 | 의미 |
|---|---|---|
| 빨강 원 (안쪽) | `interceptRadius` 0.7m | 루즈볼이면 그냥 뺏김 |
| 주황 원 | `Tackle.Reach` 1.0m | 태클 조건 ① 사거리 |
| 노랑 원 (바깥) | `lungeRange` 1.9m | 여기서부터 덤빌 수 있음 |
| 초록/빨강 회랑 | `laneHalfWidth` ±2m | 패스 레인. 빨강 = 지금 막혀 있음 |
| 회색 회랑 앞부분 | `carrierShadow` 2.5m | **판정하지 않는 구간** |
| 파랑 웨지 | `coneHalfAngle` ±15° | 지금 패스하면 갈 방향 |

**모든 숫자는 소유 컴포넌트에서 읽는다. 복사하지 않는다.** 자기 사본을 들고 있는 디버그
뷰는 없느니만 못하다 — 언젠가 설명하려는 대상과 어긋나는데 사람은 그림을 믿는다.

**주의 — 풀은 도메인 리로드에서 살아남지 못한다.** `pool` 리스트는 직렬화되지 않아
리로드 때 비워지는데 자식 `LineRenderer` 오브젝트는 씬에 남는다. 아무도 끌 수 없는
고아들이 마지막 프레임을 얼린 채 실제 그림 위에 겹쳐 그려진다. `Awake`에서
`AdoptStrays()`로 회수해야 한다. (실제로 30개가 겹쳐 그려지고 있었다.)

### 3.21 패스 벤치 — 움직이는 그림에서는 아무것도 증명할 수 없다

"수비수가 서 있는데도 그리로 패스한다"는 말은 **네 가지 서로 다른 고장**을 가리킬 수 있고,
경기 화면은 너무 빨라서 그 넷을 구분해주지 않는다.

1. 레인 판정이 사람이 서 있는 길을 열려 있다고 읽었다 — **기하가 틀렸다**
2. 고를 때는 열려 있었고, **패서가 몸을 돌리는 0.9초** 사이에 닫혔다
   (`TeamAttack.maxTurnWait`) — 판단이 틀린 게 아니라 **낡았다**
3. 레인은 열려 있었는데 **킥이 그 회랑을 벗어났다** (§3.8의 방향·강약 오차)
4. 전부 막혀서 롱볼로 넘어갔다 — 그건 허용된다. **높이가 합법성을 만든다**(§3.17)

그래서 `PassLab`은 **전원을 정지시키고 패스만** 시킨다. 아무도 안 움직이니 2번이 사라지고,
`perfectStrike`가 켜져 있으면 공이 **실제로 판정한 회랑을 그대로** 날아가므로 3번도 사라진다.
남는 것은 선택 기준 자체다. 그리고 화면에 **논쟁을 끝내는 숫자 한 쌍**을 띄운다.

```
계획 레인    치기 전에 LaneClearance()가 말한 값
실제 최근접  공이 머리 높이 아래에서 수비수에게 실제로 얼마나 가까이 갔는가
```

- 둘이 **일치하는데 여전히 몸을 통과**하면 → 게이트 임계값이 틀렸다
- 둘이 **어긋나면** → 기하가 틀렸다
- 둘이 일치하고 아무도 안 스치면 → **선택은 옳다.** 고장은 위쪽에 있다.
  `unfreezeDefence` → `perfectStrike` 순으로 하나씩 풀어서 어디서 되살아나는지 본다

**실측 (2026-09-06, 전원 정지 + 정확한 킥):** 9회 패스, 몸을 통과한 땅볼 **0회**.
계획 레인 대 실제 최근접이 7.6→7.6, 7.8→7.8, 5.6→6.0, 6.0→5.4로 붙는다.
수비수를 레인 한가운데로 옮기면 그 후보가 즉시 `레인 막힘`으로 떨어지고 다른 사람이 이긴다.
**즉 §3.17의 판정과 기하는 정상이다.** 남은 용의자는 2번(0.9초 커밋)과 3번(킥 오차),
그리고 애초에 레인 판정을 하지 않는 **플레이어의 웨지 패스**(§3.19)다.

**벤치는 아무것도 결정하지 않는다.** 코디네이터(`TeamAttack`/`TeamDefence`/`DrillDirector`)를
끄고 선수들의 `active`를 내렸다가 **빌린 그대로 돌려준다.** 출력하는 모든 숫자는
`PassPlanner`에서 읽어온 것이지 여기서 다시 계산한 것이 아니다 — 자기 산술을 하는 벤치는
자기 자신을 측정한다(§3.20과 같은 이유).

정지 상태이므로 **씬 뷰에서 선수를 끌어다 놓으면 표가 즉시 다시 채점된다.** 특정 배치를
만들어놓고 "왜 이 패스를 골랐는지" 묻는 게 이 도구의 본래 용도다.

### 3.22 패스 속도를 올리는 손잡이는 `arrivePace` 하나뿐이다

패스 속도는 "얼마나 세게 차는가"로 입력하지 않는다. **"도착할 때 몇 m/s로 굴러오면
좋겠는가"를 입력하면 `Ball.SpeedToReach()`가 초기 속도를 거꾸로 푼다**(§3.6).

```
v0 = √(arrivePace² + 2aD)
T  = (v0 - arrivePace) / a          a = rollDecel = 5 m/s²
```

그래서 **`arrivePace`가 비행 전체를 들어올린다.** 마지막 1m만 빨라지는 게 아니다.

| 거리 | 3.5 m/s (이전) | **9 m/s (현재)** |
|---|---|---|
| 5 m | 0.88초 | **0.49초** |
| 12 m | 1.60초 | **1.04초** |
| 18 m | 2.07초 | **1.43초** |
| 30 m | 2.83초 | **2.10초** |

18m 패스가 2.07초였다. 실제 축구는 1.2~1.5초다. 모래 위를 구르는 것처럼 보인 이유가
이것이고, 레인이 열려 있어야 하는 시간도 그만큼 길었다.

**주의 — 구름 감속(`rollDecel`)을 낮추면 패스가 오히려 느려진다.** 직관과 반대다.
마찰이 약하면 같은 지점에 같은 페이스로 도착시키는 데 필요한 초기 속도가 **줄어들기**
때문에 `T = (v0 - arrivePace)/a`가 길어진다. 공이 오래 굴러가는 것과 빨리 도착하는 것은
다른 이야기다. 속도를 올리고 싶으면 `rollDecel`이 아니라 `arrivePace`를 만질 것.

**`riskTime`은 `arrivePace`에 종속이다** (§3.17). 위험항은 비행 시간을 `riskTime`으로
나눠 0~1로 만드는데, 상한이 실제 최장 비행보다 한참 위에 있으면 모든 후보가 낮은
쪽에 뭉쳐서 항이 다시 죽는다 — 3.2초로 두면 5~30m가 전부 0.15~0.66에 몰린다.
2.4초에서 0.20~0.88로 벌어진다. **한쪽을 바꾸면 반드시 다른 쪽을 다시 잰다.**
패스 벤치(§3.21)를 켜면 이 표가 화면에 그대로 나온다.

### 3.23 몸 — 판단이 맞아도 움직임이 틀릴 수 있다

봇 이동이 한 줄이었다. 목표를 향해 고정 가속, 클램프. 전략 게임 유닛처럼 움직였다 —
**전속력에서 즉시 방향 전환**, 최고 속도로 도착해서 제자리 진동, 후진도 전진과 같은 속도.
이건 판단 문제가 아니다. **어디로 갈지 맞춰도 가는 모습이 틀릴 수 있다.**

핵심 아이디어 하나: **속도 변화량을 "진행 방향"과 "옆 방향"으로 쪼갠다.**

달리던 방향으로 빨라지거나 느려지는 건 싸다. 옆으로 미는 건 비싸고, 그걸 제한하면
**아무도 원호를 계산하지 않아도 회전 반경이 생긴다.**

```
회전 반경 = 속도² / turnAccel
7.9 m/s ÷ 12 m/s²  →  5.2m      ← 전속력에서 이 안쪽으로는 못 돈다
```

나머지는 전부 같은 정직함의 결과다. 브레이크가 가속보다 세고(멈추는 게 출발보다 쉽다),
도착은 미리 감속하는 것이고, 뒤로 뛰는 건 느리다.

**실측:**

| | |
|---|---|
| 정지 → 전속력 | 0.80초 |
| 전속력 → 정지 | 0.50초 / 1.9m |
| 전속력 90도 커트 | 코너를 **2.5m 지나쳐서** 돈다 |

**주의 — 180도 전환은 회전 문제가 아니라 제동 문제다.** 정확히 뒤를 향한 목표는 옆 성분이
0이라 turnAccel이 전혀 관여하지 않는다. 처음에 brake=34로 뒀더니 0.22초 만에 반대로
뛰었다. 회전 반경만 보고 튜닝하면 이 경로를 놓친다.

### 3.24 공간 지배 격자 — 몸 개수가 아니라 시간을 묻는다

이전의 모든 질문이 **몸**에 대한 것이었다. 가장 가까운 수비수까지 거리, 이 칸의 수비 숫자.
그런데 **몸 개수는 그 공간에 서 있는 사람과 그 공간에서 빠져나가는 중인 사람을 구분하지
못한다.** 그래서 곧 채워질 방으로 패스하고, 곧 비워질 방을 무시했다.

`PitchControl`은 **시간**을 묻는다. 피치의 각 칸마다 "우리가 먼저 도착하나, 저쪽이 먼저
도착하나". 그 칸으로 달려오는 사람은 그 칸에 서 있는 사람보다 **더 멀리서도 그 칸을
소유한다** — 이게 축구에서 공간이 작동하는 방식이고, 거리로는 표현할 수 없다.

```
도달 시간 = reaction + |칸 - (위치 + 속도 × momentum)| / 속도
지배도    = squash( (저쪽시간 - 우리시간) / decisiveness )      → -1 .. +1
```

`momentum`(0.45초)이 핵심이다. 발이 아니라 **관성이 데려다줄 곳**에서 재기 때문에
달리는 선수가 앞 공간을 소유한다.

**격자 하나가 세 가지 질문에 답한다** — 예전엔 각각 다른 코드였다:

| 질문 | 답 |
|---|---|
| 오프더볼 선수는 어디 설까 | 우리가 가장 잘 지배하는 칸 |
| 이 패스는 공간으로인가 곤경으로인가 | 도착점의 지배도 |
| 다 막혔을 때 어디로 띄우나 | 가장 비어 있는 구역 |

**검증 (34×53 격자):**

```
홈 진영 (0,-25) = +0.81      상대 박스 (0,45) = -0.69      공 근처 = +0.23 (경합)
```

패스 후보의 `freedom` 항이 지배도를 그대로 따라간다:

```
Home_LB   ctrl +0.53 → freedom 0.93
Home_RCM  ctrl -0.39 → freedom 0.31
```

각 팀이 **자기 스냅샷으로 자기 격자를 만든다**(§3.15). 공용 격자를 쓰면 두 팀이 서로의
속을 읽게 되어 지연이라는 난이도 장치가 무너진다.

**두 격자는 거울상이 아니다 — 그게 핵심이다.** 각자 다른 순간의 스냅샷으로 지었으므로
피치 전체 평균 `|공격 + 수비|`가 **0.048**로 나온다. 0이면 완전한 거울상(= 서로의 속을
읽는 중)이고, 이 어긋남이 **둘 중 하나가 곧 틀릴 지점**이다.

### 수비의 격자는 읽는 법이 다르다

공격은 지배도를 **그대로** 쓴다 — 높으면 거기로 넣는다. 수비가 같은 질문을 하면
쓸모없는 답이 나온다. 수비가 가장 못 지배하는 땅은 보통 **코너 플래그**이고 거기를
내주는 건 아무 일도 아니기 때문이다.

그래서 수비는 `TeamDefence.ExposureAt()`로 읽는다 — "우리가 어디를 먹었나"가 아니라
**"쟤네가 먹은 땅 중 어디가 아픈가"**다.

```
ExposureAt(p) = (1 − OursAt(p)) × Danger(p)      ← 실점 비용으로 가중
```

`Danger()`는 마킹 우선순위가 이미 쓰고 있는 그 함수다(§3.15). 실측:

| 지점 | 수비 지배도 | ExposureAt |
|---|---|---|
| 홈 진영 −25 | −0.79 | **0.00** ← 남의 동네, 관심 없음 |
| 원정 박스 +45 | +0.71 | 0.14 ← 자기 박스는 자기가 잡고 있음 |
| 원정 코너 | +0.34 | 0.02 ← 내줘도 안 아픔 |
| **최악 지점 (−5, +31)** | | **0.37** ← 박스 앞 하프스페이스 |

**"우리 편"은 `members`(필드 플레이어)만이다.** 골키퍼는 두뇌가 없어 애초에 이 배열에
없는데, 결과적으로 그게 맞다 — **골키퍼가 하프라인 경주에서 이긴다고 계산되면** 수비가
갖지도 않은 땅을 가진 것으로 친다.

**아직 아무 행동도 이 지도를 소비하지 않는다.** 만들고 눈으로 볼 수 있게 한 데까지다.
붙일 때 지켜야 할 선이 하나 있는데 — **§3.18의 차단 판정에는 넣지 말 것.** 실시간인 것은
차단뿐이고 나머지가 1초 늦는 게 난이도 장치인데, 지도가 편하다고 거기 끼워 넣으면
그 장치가 조용히 무너진다.

`PlayDebugView`가 잔디에 직접 칠한다 — 파랑이 우리 땅, 빨강이 저쪽 땅, **흐릿한 가운데가
진짜 경합 중인 곳**이고 보통 볼 가치가 있는 건 거기뿐이다. 선 1800개가 아니라 텍스처
한 장(칸당 1픽셀, Point 필터)이다. `controlView`로 **공격 / 수비 / 수비-위협** 세 장을
갈아 끼운다 (인스펙터, 플레이 중에도 됨).

---

## 4. 파일 구조

전부 `Assets/_Prototype/` 아래.

| 파일 | 역할 |
|---|---|
| `Scripts/ProtoCore.cs` | `SightState`, `ProtoMat`(머티리얼 헬퍼), **`ProtoInput`**(키 바인딩 전부), **`PerceptionSystem`**(시야 판정) |
| `Scripts/Perceivable.cs` | 인지 대상. 안 보이면 렌더러 끄고 잔상 표시 |
| `Scripts/FootballerController.cs` | 이동·몸통/시선 분리·어깨 확인·리시브 구속 |
| `Scripts/Formation.cs` | 4-1-2-3 슬롯 좌표 (데이터만, AI 없음). `Role`, `FormationSlot`, `WorldPos` |
| `Scripts/TacticalPitch.cs` | 5레인 구분 + 위험도 `Danger()`. 콜라이더 아님, 순수 산술 |
| `Scripts/PitchIntel.cs` | 팀이 '믿고 있는' 그림. 1초 텀 스냅샷 (공수 공용) |
| `Scripts/TeamDefence.cs` | 수비 **공통 원칙** 6가지 + 패스 차단 지점 계산 |
| `Scripts/DefenceDuties.cs` | **포지션별로 누구를 맡는가.** 순수 함수, 상태 없음 |
| `Scripts/Offside.cs` | 오프사이드 라인 = 뒤에서 두 번째 수비수 (공·하프라인 max) |
| `Scripts/TeamAttack.cs` | 공격 5원칙(점수) + **공 소유·패스 실행·루즈볼 수거** |
| `Scripts/PassPlanner.cs` | **누구에게 보낼 것인가.** 레인 판정 / 롱패스 / 플레이어 웨지 |
| `Scripts/AttackerAI.cs` | 오프더볼 실행 + 침투/흔들기 타이머 |
| `Scripts/Ball.cs` | 구름 마찰 + **포물선 체공** + 드리블 소유 + `Predict()` |
| `Scripts/BallModel.cs` | 스탯 → 속도/방향오차/강약오차 |
| `Scripts/Tackle.cs` | 태클 2조건 판정 + `Probe`(HUD용) |
| `Scripts/DefenderAI.cs` | 압박 유지 / 볼 추격 / 태클 커밋 |
| `Scripts/DrillDirector.cs` | 경기 루프, 플레이어 터치·패스, 결과 판정, HUD |
| `Scripts/PitchCamera.cs` | 고정 요각 직교 추적 카메라 |
| `Scripts/VisionOverlay.cs` | 셰이더 전역 변수 갱신 + 인지 시스템 구동 |
| `Scripts/PlayerMotion.cs` | **몸이 할 수 있는 것.** 가감속·회전 반경·후진 (§3.23) |
| `Scripts/PitchControl.cs` | **각 칸을 누가 먼저 잡는가.** 공격·수비 각각 하나씩 (§3.24) |
| `Scripts/PlayDebugView.cs` | **판정 거리와 지배 격자를 잔디에 그린다** (G). 읽기 전용 |
| `Scripts/PassLab.cs` | **패스 벤치** (P). 전원 정지 후 패스만 — 계획 레인 대 실제 최근접 (§3.21) |
| `Shaders/VisionOverlay.shader` | 3단계 시야 암전 |
| `Editor/PrototypeSceneBuilder.cs` | **씬 전체를 코드로 생성** |

### 씬 재생성

```
Tools > Football Prototype > Build Drill Scene
```

씬은 손으로 편집하지 않는다. 피치 규격, 마킹, 선수 크기, 카메라, 컴포넌트 배선이 전부 빌더에 있다. **빌더가 단일 진실 공급원이다.** 스크립트 기본값을 바꿔도 빌더가 덮어쓰는 값이 있으니 양쪽을 맞출 것.

---

## 5. 조작 (키보드 전용)

오른손 방향키, 왼손 WASD. 패드 매핑은 FC와 동일 (패스 ✕, 슛 ○, 크로스 □, 스루 △).

| 키 | 패드 | 동작 |
|---|---|---|
| 방향키 | 좌스틱 | 이동 · **패스 방향 지정**(§3.19) |
| W | Y (△) | 스루패스 요구 |
| A | X (□) | 크로스 |
| S | A (✕) | 발밑 패스 요구 · 레이오프 |
| D | B (○) | 슛 |
| Q | — | 어깨 너머 확인 (홀드) |
| E | LT | 몸으로 지키기 |
| Shift | RB | 스프린트 |
| LeftCtrl | LB | 제자리 고정 (방향만 지정) |
| G | R3 | 수비 범위·패스 레인 표시 (§3.20) |
| R / Tab | — | 리셋 / 시야제한 토글 |
| P | — | **패스 벤치** — 전원 정지 후 패스만 (§3.21) |
| Space / B | — | 벤치 전용: 지금 패스 / 공을 센터백에게 되돌림 |

마우스 관련 코드는 `// [MOUSE]` 마커로 **주석 처리**되어 있다 (삭제 아님). 되살리려면 마커를 찾을 것.

### 어깨 너머 확인(Q) 규칙

누르는 순간 어느 쪽을 볼지 결정하고, 홀드하는 동안 `scanAngle`만큼 고개가 돌아간다.

1. **첫 확인** → 양 골대 연결선이 나눈 면적 중 넓은 쪽
2. **두 번째** → 반대쪽
3. **세 번째부터** → 상대를 목격한 쪽. 미확인이면 계속 번갈아
4. **터치라인 5m 이내** → 항상 넓은 쪽(안쪽). 번갈아보기 없음
5. **시선이 터치라인을 넘지 않음** → 넘는 각도는 라인과 평행하게 클램프

어느 어깨로 도는지는 `side = sign(원하는 피치쪽 × cos(몸 yaw))`로 자동 결정된다.

---

## 6. 경기 루프

드릴은 없어졌다. **남긴 것과 이유:**

| 남긴 것 | 이유 |
|---|---|
| 센터백의 패스로 시작 | 등지고 받는 그 상황이 시야 메커닉이 존재하는 이유다. 다른 데서 시작하면 그 장면이 안 나온다 |
| 리시브 구속·캐리·태클·슈팅 | 손대지 않았다 |
| 확인/블라인드 두 숫자 | §1 그 자체. 판정 기준만 바뀌었다 |

**없앤 것: 공이 플레이어에게 배달되지 않는다.** 그는 열한 명 중 하나이고, 라운드가 그와
무관하게 끝날 수도 있다.

```
Setup → Play → Result
```

- **Setup** — 전원 포메이션 슬롯 복귀, 공은 센터백 발밑
- **Play** — 봇끼리 패스(§3.17), 수비는 차단 시도(§3.18), 플레이어는 후보 중 한 명.
  공이 그에게 오면 기존 리시브 구속이 그대로 걸리고, 잡으면 캐리로 들어간다
- **Result** — INTERCEPTED / TACKLED / OUT / GOAL / FREE KICK / DEAD BALL / KEPT IT

**§1 숫자의 판정 기준이 바뀌었다.** 6초 타이머가 아니라 **그의 터치 이후 소유권이 실제로
어떻게 됐는가**다.

```
그의 패스를 팀원이 잡음        → 지킴
그의 패스가 끊김 / 라인 아웃    → 뺏김
캐리 중 태클당함               → 뺏김
슛이 골문 안                   → 지킴
```

`armed` 플래그로 **그가 실제로 공을 만진 라운드만** 집계한다. 반대편 끝에서 무너진
소유권이 이 숫자를 오염시키면 안 된다.

---

## 7. 핵심 수치

### 규격 (FIFA 권장)

피치 105 × 68m (halfX 34, halfZ 52.5) · 센터서클 9.15 · 페널티박스 40.32×16.5 · 골에어리어 18.32×5.5 · 페널티마크 11 · 골대 7.32×2.44 · 코너아크 1
선수 1.80m (지름 0.56) · 공 반지름 0.11 (5호)

### 튜닝 포인트

| 항목 | 값 | 파일 |
|---|---|---|
| 이동 / 스프린트 | 5.6 / 7.9 m/s | FootballerController |
| 봇 가속 / 제동 | 10 / 16 m/s² | MotionModel |
| 봇 횡가속 (회전 반경) | 12 m/s² → 5.2m @7.9 | MotionModel |
| 지배 격자 칸 | 2m (34×53) | PitchControl |
| 관성 반영 | 0.45초 | PitchControl |
| 어깨 확인 각도 | 140° | FootballerController |
| 시야 중심/주변/말초 | 26° / 55° / 80° | ProtoCore |
| 시야 최대 거리 | 30m | ProtoCore |
| 잔상 유지 | 4.5초 | ProtoCore |
| 구름 감속 | 5 m/s² | Ball |
| **패스 도착 페이스** | **9 m/s** (= 패스 속도 조절 knob) | PassRules |
| 위험항 포화 시간 | 2.4초 (도착 페이스에 종속) | PassRules |
| 패스 레인 반폭 | 2 m | PassRules |
| 땅볼 패스 한계 | 30 m | PassRules |
| 플레이어 패스 웨지 | ±15° | PassRules |
| 롱패스 정점 | 거리 × 0.16 (3~9 m) | PassRules |
| 백라인 하한 | 자기 골라인 +8m | TeamDefence |
| 마킹 목줄 | 26m | TeamDefence |
| 영역 밖 = 주시만 | blend 0.35 | TeamDefence |
| 포지션 목줄 | CB 9 / FB 12 / MF 13 / W 15 / ST 16 m | Formation |
| 역습 지연 간격 | 3.2m | TeamDefence |
| 전방 3명 하강 | 9m (약한 압박) | TeamDefence |
| 남는 CB 반응 위험도 | 0.30 | DefenceDuties |
| 차단 반응 시간 | 0.28초 | TeamDefence |
| 차단 가능 높이 | 1.1 m | TeamDefence |
| 패스 이탈 거리 | 1.4 m | DrillDirector |
| 터치 거리 근/원/곡선 | 0.40 / 1.45 / 2.5 | Ball |
| 발밑 리드 계수/상한 | 0.40 / 2.0m | DrillDirector |
| 스루 리드 계수/범위 | 1.25 / 5~14m | DrillDirector |
| 스루 따라잡기 기준 | 공 페이스의 40% | DrillDirector |
| 버티기 목표 | 6초 | DrillDirector |
| 태클 사거리 / 실드 반경 | 1.0 / 0.45m | Tackle |
| 헤비터치 판정 | 0.9m | DefenderAI |
| 태클 쿨다운 / 넘어짐 | 1.1 / 1.0초 | DefenderAI |
| 방향오차 계수 | 5.0m | BallModel |
| 정보 갱신 텀 (평시/급박) | 1.0 / 1.8초 | PitchIntel |
| 급박 판정 거리 | 블록 중심 12m | PitchIntel |
| 블록 추종 (횡/종) | 0.55 / 0.60 | TeamDefence |
| 라인 내 인접 간격 | 12m (튜닝 중) | TeamDefence |
| 라인 간 간격 | 14m | TeamDefence |
| 간격 완화 반복 / 감쇠 | 16회 / 0.8 | TeamDefence |
| 마킹 발동 위험도 | 0.34 | TeamDefence |
| 마커 최대 이동 | 26m | TeamDefence |
| 커버 블렌드 | 0.55 (구멍당 1명) | TeamDefence |
| 압박 간격 / 안쪽 어깨 | 1.05 / 0.85m | DefenderAI |
| 태클 발동 (밀착/전환각) | 1.5m / 75° | DefenderAI |
| 패스 거리 밴드 | 7~24m | TeamAttack |
| 열린 레인 기준 / 캐리어 그림자 | 2.4m / 2.5m | TeamAttack |
| 막힘 판정 레인 | 2.6m | TeamAttack |
| 이상적 삼각형 각도 | 60° | TeamAttack |
| 온사이드 여유 | 0.7m | TeamAttack |
| 존 반경 (윙·ST / 8번 / 풀백 / CB) | 11 / 9 / 8 / 5m | Formation |
| 침투 주기 / 지속 | 5~11초 / 1.8초 | AttackerAI |
| 흔들기 방향 전환 주기 | 1.4초 | AttackerAI |
| 강약오차 계수 | 0.18 | BallModel |
| 속도 밴드 | ±15% | BallModel |

### 시야 암전 (`Materials/VisionOverlay.mat`)

DarkColor (0,0,0) · PeripheralDim 0.58 · EdgeDim 0.93 · BlindDim 0.972 · FocusSoft 9 / PeriphSoft 11 / EdgeSoft 14

---

## 8. 다음 단계

우선순위 순.

1. **§1 검증 숫자 확인** — 20~30 라운드. 격차가 안 나면 시야 설계를 다시
2. **공간 오디오** — 발소리·호흡의 방향. 재미 기여도가 가장 클 것으로 예상
3. **`TacticalPitch`** — 5레인 × 6밴드 그리드. 사이드라인 로직을 여기로 옮기고 AI 기반 마련 (§3.12)
4. **FM 능력치 연결** — Vision → 시야각, Anticipation → 잔상 시간, Composure → 압박 시 시야 축소
5. **공중볼** — 크로스·헤딩. 회전은 곡률로 (§3.7)
6. **22명 AI** — 3번이 선행되어야 함

### 미해결 / 검토 필요

- 신발끈 면적 계산을 2줄 공식으로 축약 (§3.14)
- 받는 순간 W(스루)가 지금은 레이오프와 동일 처리됨. 상황별 바인딩 설계 시 정리
- Space 키가 비어 있음 (엄지로 누르기 가장 좋은 자리)
- 필드가 105m가 되면서 드릴이 센터서클 안쪽에서만 벌어짐. 측면으로 옮기면 사이드라인 규칙이 실제로 발동

---

## 9. 작업 규칙

### 9.1 요구한 것이 이미 다른 형태로 구현되어 있으면, 먼저 알리고 선택지를 물어본다

수정 요청을 받으면 **코드를 건드리기 전에** 그 기능이 이미 존재하는지 확인한다.
이미 있다면 바로 고치지 말고, 다음 세 가지를 보고한 뒤 선택을 기다린다.

1. **지금 어떤 형태로 구현되어 있는가** — 파일과 줄 번호
2. **요청한 값·동작과 실제로 무엇이 다른가** — 차이가 없으면 "차이 없음"이라고 명시
3. **선택지** — 예: 그대로 둔다 / 값만 바꾼다 / 구조를 바꾼다(단일 출처로 통합 등)

**왜:** 2026-08-30, "공 크기를 FIFA 규격 69cm로 수정" 요청에 확인 없이 바로 착수했다.
그런데 기존 값 `radius = 0.11`(지름 0.22m)은 둘레로 환산하면 69.1cm — 이미 규격이었다.
실제 변경량은 지름 0.37mm였고, 되돌리기까지 왕복이 발생했다.
심지어 이 문서 §7에 `공 반지름 0.11 (5호)`라고 이미 적혀 있었다.
확인이 먼저였다면 "이미 5호 규격입니다. 그래도 정확히 69.0cm로 맞출까요?" 한 줄로 끝났다.

**적용 범위:** 규격·수치 변경, 이미 있는 기능의 재구현, 리팩터링 요청.
특히 §7 튜닝 포인트 표에 있는 값은 손대기 전에 이 문서를 먼저 읽는다.

**예외:** 차이가 명백하고 큰 경우(예: 5.6 → 8.0)는 묻지 않고 진행한다.
이 규칙은 "이미 되어 있는 것을 모르고 다시 하는 것"을 막기 위한 것이지,
매번 확인을 받으라는 뜻이 아니다.
